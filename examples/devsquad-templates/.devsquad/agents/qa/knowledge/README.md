# qa knowledge

| 파일 | 내용 | 언제 |
|---|---|---|
| `e2e-env.md` | E2E 실행 환경 (base URL 변수명, 테스트 계정 생성 방법 — 값은 넣지 않음), 시드 API | 항상 |
| (추가) `fixtures.md` | 재사용 가능한 fixture·헬퍼 목록 | 항상 |

## 주의
테스트 계정 비밀번호, 토큰, URL 실제 값은 여기 넣지 않는다. 환경 변수 이름만 적는다 (`E2E_BASE_URL`, `E2E_USER_EMAIL`).
