# backend knowledge

| 파일 | 내용 | 언제 |
|---|---|---|
| `api-guidelines.md` | URL·메서드·상태 코드·페이지네이션·버저닝 규칙 | 항상 |
| `error-codes.md` | 표준 오류 응답 형식과 코드 목록 | 항상 |
| `db-schema.md` | 현재 스키마 요약 (테이블, 핵심 컬럼, 관계). `schema.sql` 덤프가 있으면 그것을 대신 넣어도 됨 | 항상 |
| (추가) `adr/*.md` | 아키텍처 결정 기록 (왜 이 패턴인가) | 구조 판단 시 |
| (추가) `security.md` | 인증·인가 방식, 개인정보 취급 규칙 | 사용자 데이터 다룰 때 |

## 넣어야 할 것
- 실제 스키마 (LLM은 우리 테이블을 모른다)
- 프로젝트 고유의 오류 코드 체계
- 외부 시스템 연동 계약 (결제, 알림 등)

## 넣지 않아야 할 것
- Spring 일반 문서
- 접속 문자열, 키, 내부 IP
